commands {
    "promote" {
        alias="promote {0}"
        commands=[
            "lp user {0} promote default"
        ]
        permission="alias.promote"
    },
    "demote" {
        alias="demote {0}"
        commands=[
            "lp user {0} demote default"
        ]
        permission="alias.demote"
    }
}
